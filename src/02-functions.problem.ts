/**
 * Задача 2: Функции
 * 
 * Проблема: Создайте типизированную функцию calculateArea, которая принимает
 * параметры width и height и возвращает площадь прямоугольника.
 * Функция должна работать как с целыми числами, так и с дробными.
 */

// TODO: Реализуйте функцию calculateArea с правильными типами параметров и возвращаемого значения

// Проверка:
const area1 = calculateArea(10, 5);     // должно работать с целыми числами
const area2 = calculateArea(10.5, 5.2); // должно работать с дробными числами 